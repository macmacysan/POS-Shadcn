import LoginForm from '@/components/LoginForm'

function App(): React.JSX.Element {
  return (
    <main className="flex min-h-screen w-full items-center justify-center bg-background/80 px-4 py-8">
      <LoginForm />
    </main>
  )
}

export default App
